// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface TableInfo {
  id: number;
  requestType: string;
  teamName: string;
  division: string;
  organization: string;
  nomination: string;
  status: string;
}
